/ - time = Time.now.to_s => moved to controller

%h1= "Hello from Rails #{@date}"

= 1 + 2
= "cow" + "boy"
/ Time setted in config/application.rb
/ All available time zones: rake time:zones:all 
= 1.hour.from_now

- for file in @files
	%ul
		%li= file

%h2
	="Calendar: "
	=@time

- @calendar.split("\n").each do |day|
	%p= day

%p
	/ %a{href: "/say/goodbye"} good bye!	=> bad practice
	=link_to "good bye!", say_goodbye_path